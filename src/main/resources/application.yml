logging:
  level:
    root: INFO

server:
  port: 8080
  forward-headers-strategy: framework

spring:
  mvc:
    problemdetails:
      enabled: true

springdoc:
  swagger-ui:
    use-root-path: true
    try-it-out-enabled: true
    csrf:
      enabled: false