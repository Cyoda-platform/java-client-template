The project compilation check was executed successfully with no errors found after the fixes were applied.

Summary:
- Initially, the project failed to compile due to extra closing braces in some entity files.
- Those were fixed by removing the extra braces.
- Then, further compilation errors related to undefined method getTechnicalId() and type mismatch in processor classes were fixed by replacing the method calls with existing fields and adjusting CompletableFuture types.
- After these fixes, the project compiled successfully with no errors.

I have called the run_github_action 3 times as per the constraint, so the task is complete.