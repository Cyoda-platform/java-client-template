Summary of execution

- I called the GitHub Actions workflow (run_github_action) 3 times (max allowed). Each call failed immediately with the same error:
  - Error: GH_TOKEN not configured in environment variables
- Because the GH_TOKEN is not configured in the environment, the build workflow could not run and no compilation output was produced by CI. I could not obtain a successful compilation result.

What I changed while diagnosing (allowed paths only)
- I inspected entity and processor/criterion source files under:
  - src/main/java/com/java_template/application/entity/**
  - src/main/java/com/java_template/application/processor/**
  - src/main/java/com/java_template/application/criterion/**
- I applied fixes/enhancements within the allowed directories to address potential compile/runtime problems and to complete processor/criterion implementations:
  - Added an extensible metadata field to Order entity:
    - File modified: src/main/java/com/java_template/application/entity/order/version_1/Order.java
    - Added: private Map<String,Object> metadata;
    - This prevents runtime ClassCast/NullPointer issues where processors expect metadata to exist and to be a Map.
  - Updated/rewrote processors to use concrete entity inner classes and consistent types:
    - ValidateOrderProcessor: ensured it reads/writes metadata safely and uses entity getters/setters only.
    - CreateShipmentProcessor: rewrote version to use Shipment.ShipmentItem and Order.OrderItem types, and to safely persist shipments via EntityService.addItem.
    - Re-saved CreatePaymentProcessor and PaymentAuthorizationProcessor (no API changes other than safe metadata handling).
  - All changes were confined to:
    - src/main/java/com/java_template/application/entity/**
    - src/main/java/com/java_template/application/processor/**
    - src/main/java/com/java_template/application/criterion/**

Why CI failed
- The workflow runner requires a GH_TOKEN environment variable to be configured for the GitHub Actions invocation in this environment. The run_github_action tool returns the error above before any compile step runs. Therefore, no compile logs are available and I could not confirm whether the project compiles.

Current state & next steps for you
1. Provide GH_TOKEN in the environment used to run the workflow (preferred) so I (or CI) can run the build and produce compilation results.
   - Once GH_TOKEN is available I can re-run the build and fix any remaining compilation errors (we have up to 0 more run_github_action calls left in this session).
2. Alternatively, run locally:
   - From a dev machine with Java 21 and Maven installed, run:
     - mvn -DskipTests=false clean compile
   - If compilation fails, paste the mvn output here and I will propose precise fixes.
3. Notes about the code changes I made:
   - Metadata was added to Order to avoid ClassCast/NullPointer hazards processors expected.
   - CreateShipmentProcessor now creates ShipmentItem instances (matches Shipment POJO inner type) and uses EntityService.addItem as required.
   - ValidateOrderProcessor improved to use existing getters and safe metadata handling.
   - No files outside the permitted directories were touched.

Final status
- run_github_action called the allowed maximum 3 times.
- All attempts failed due to missing GH_TOKEN in the environment; build/compile could not be executed.
- I updated Java files (only in the allowed directories) to address likely issues and to complete processor/criterion logic so they are more likely to compile successfully when CI is runnable.

If you can provide GH_TOKEN or allow another CI run, I will immediately re-run the workflow and fix any compilation errors encountered. If you prefer, share the full mvn compile output and I will produce targeted code fixes.