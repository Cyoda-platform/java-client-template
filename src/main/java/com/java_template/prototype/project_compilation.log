Summary of actions performed

1) Initial compilation check
- I ran the provided GitHub Actions workflow (./.github/workflows/build.yml) once.
- Result: compile failed with 14 errors (javac) — root cause: entity classes were missing fields/getters/setters used by processors/criteria (errors like cannot find symbol: method getTechnicalId(), setDecisionAt(Instant), setReviewer(String), etc., coming from AdoptionNotifyProcessor, AdoptionAssignReviewerCriterion processor/criterion, PetValidationCriterion).

2) Fixes applied
- I updated the entity classes so they expose the fields that processors/criteria expect (so Lombok @Data will generate the getters/setters).
- Files modified:
  - src/main/java/com/java_template/application/entity/adoptionrequest/version_1/AdoptionRequest.java
    - Added fields: technicalId (String), decisionAt (Instant), reviewer (String)
    - Kept existing fields; class remains annotated with @Data and implements CyodaEntity/isValid()
  - src/main/java/com/java_template/application/entity/pet/version_1/Pet.java
    - Added field: technicalId (String)
    - Kept existing fields; class remains annotated with @Data and implements CyodaEntity/isValid()

(These edits ensure methods like getTechnicalId(), setDecisionAt(Instant), setReviewer(String) exist via Lombok.)

3) Verification (re-run)
- I ran the GitHub Actions workflow again to verify the fixes.
- Result: BUILD SUCCESSFUL
  - The workflow log contains: "✅ Compilation successful - no errors found"
  - Gradle: "BUILD SUCCESSFUL in 20s" and 8 actionable tasks executed.

Run counts and status
- run_github_action calls used: 2 (initial failing run + verification run)
- Remaining allowed calls: 1 (max 3)
- Task status: Completed — project now compiles successfully.

What I changed (concise)
- Added the missing technicalId, decisionAt, reviewer fields to the entity classes used by processors/criteria so that the code relying on req.getTechnicalId(), req.setDecisionAt(...), req.setReviewer(...) compiles correctly.
- No changes were made to processors/criteria themselves.

Next steps / notes
- If you want, I can:
  - Commit a detailed PR description / commit message summarizing the change.
  - Run the full build (tests) in CI if you want beyond compile-only.
  - Replace Lombok-generated fields with explicit getters/setters if you prefer not to rely on Lombok.
- Build warnings: Gradle reported some deprecation warnings (Gradle 8.7 -> future Gradle 9), but this doesn't block compilation.

If you want the exact diffs or to review the updated file contents, tell me which file and I'll paste the final contents.