Summary of actions performed
- Step 1 — Initial compilation check: I ran the GitHub Actions build (./gradlew clean compileJava) to detect compilation failures.
  - Command executed via run_github_action (build.yml, compile-only).
  - Result: compilation failed with a javac error:
    - File: src/main/java/com/java_template/application/controller/IngestionRunController.java
    - Error: "import io.swagger.v3.oas.annotations.parameters.RequestBody as SwaggerRequestBody;" — ';' expected (Java does not support import aliasing with "as").

- Step 2 — Fix applied:
  - I updated src/main/java/com/java_template/application/controller/IngestionRunController.java to remove the invalid import alias and instead use the fully-qualified annotation reference in the controller method parameter.
  - I preserved all other logic and adhered to project patterns (constructor injection, EntityService usage, Lombok @Data inner classes).
  - The change was written via add_application_resource and committed to the repository path:
    - src/main/java/com/java_template/application/controller/IngestionRunController.java

- Step 3 — Verification:
  - I ran the GitHub Actions build again via run_github_action (build.yml, compile-only).
  - Result: BUILD SUCCESSFUL — "✅ Compilation successful - no errors found".
  - The second run produced the build success logs (compile successful, 8 actionable tasks executed).

Important details
- Number of run_github_action calls used: 2 (within the allowed maximum of 3).
- Files modified: only the controller file indicated above (under src/main/java/com/java_template/application/controller), which is within the allowed directories.
- Reason for the change:
  - The original source attempted to alias a Java import using "as", which is invalid Java syntax. Removing the alias and referencing the annotation with its fully-qualified name in the method parameter fixes the compile error while keeping the intended Swagger/OpenAPI annotation usage.

Current status
- The project compiles successfully. The most recent build run shows BUILD SUCCESSFUL and "✅ Compilation successful - no errors found".

If you want, next steps I can take
- Implement the processors and criteria required by the workflows (I have the templates and rules you provided). I can:
  - Extract workflow components and generate the needed processor and criterion classes.
  - Implement business logic based on entities and functional requirements.
- Or, if you prefer, I can open a PR with the controller fix and a brief changelog.

Let me know which next step you want to proceed with.