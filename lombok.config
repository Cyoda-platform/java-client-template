# Lombok configuration
# Disable null annotation support to prevent Lombok from adding null checks to setters
# This allows tests to set @NotNull fields to null for validation testing
lombok.addNullAnnotations = none

